{% extends 'admin_base.html.twig' %}

{% block title %}Edit Professor
{% endblock %}

{% block content %}
    <section
        class="uk-section">
        <!-- breadCrumb -->
        <div class="uk-container uk-margin">
            <ul class="uk-breadcrumb">
                <li>
                    <a href="{{path('home')}}">Home</a>
                </li>

                <li>
                    <a href="{{path('professor_index')}}">Professeurs</a>
                </li>
                <li>
                    <p>Professeur:
                        <em>{{professor.fullName}}</em>
                        _ Modification
                    </p>
                </li>
            </ul>
        </div>
        <!-- end of breadcrumb -->

        <div class="uk-container">
            <h1>Modification d'un Professeur</h1>

            {{ include('professor/_form.html.twig', {'button_label': 'Modifier'}) }}
            <div class="uk-grid uk-grid-small uk-grid-divider uk-margin-top uk-margin-bottom" uk-grid>
                <div>
                    <a class="uk-button uk-button-text" href="{{ path('professor_index') }}">
                        <span uk-icon="icon: arrow-left"></span>
                        retour à la liste</a>
                </div>
                <div>
                    <a href="#delete-confirm" uk-toggle>
                        <span uk-icon="icon: trash"></span>
                    </a>
                </div>
            </div>


            {{ include('professor/_delete_form.html.twig') }}
        {% endblock %}
