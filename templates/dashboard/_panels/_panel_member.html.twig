<div class="uk-card uk-card-default uk-card-small uk-card-hover">
    <div class="uk-card-header">
        <div class="uk-grid uk-grid-small">
            <div class="uk-width-auto">
                <h4>Elèves inscrits</h4>
            </div>
            <div class="uk-width-expand uk-text-right panel-icons">
                <a class="uk-icon-link" data-uk-icon="icon: move" data-uk-tooltip href="#" title="Move"></a>
            </div>
        </div>
    </div>
    <div class="uk-card-body">
        <table class="uk-table uk-table-hover">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>age</th>
                    <th>Email</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
                {% for member in membersPanel %}
                    <tr>
                        <td>
                            <a href="{{ path('member_show',{'slug':member.slug})}}">
                                <p class="uk-text-small">{{ member.userFullName }}</p>
                            </a>
                        </td>
                        <td>
                            <p class="uk-text-small">{{ member.older }}
                                <small>
                                    ans</small>
                            </p>
                        </td>
                        <td>
                            <p class="uk-text-small">{{ member.email }}</p>
                        </td>
                        <td>
                            <div class="uk-grid-small uk-grid-divider" uk-grid>
                                <a href="{{ path('member_show',{'slug':member.slug})}}">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a class="uk-button uk-button-link" href="{{ path('member_edit', {'slug': member.slug}) }}" uk-icon="icon:file-edit" uk-tooltip="title: Modifier {{member.userFullName}}"></a>
                            </div>
                        </td>

                    </tr>
                {% endfor %}
            </tbody>

        </table>
    </div>
</div>
