{% extends 'admin_base.html.twig' %}

{% block title %}Liste des utilisateurs
{% endblock %}

{% block content %}
    <section
        class="uk-section">
        <!-- breadCrumb -->
        <div class="uk-container uk-margin">
            <ul class="uk-breadcrumb">
                <li>
                    <a href="{{path('home')}}">Home</a>
                </li>

                <li>
                    <p>Utilisateurs</p>
                </li>
            </ul>
        </div>
        <!-- end of breadcrumb -->

        <div class="uk-container">
            <h1>Liste des utilisateurs</h1>
            <div class="uk-float-right">
                <a class="uk-button uk-button-primary" href="{{ path('user_new') }}">Create new</a>
            </div>
            <table class="uk-table uk-table-hover">
                <thead>
                    <tr>
                        <th>Avatar</th>
                        <th>Email</th>
                        <th>Roles</th>
                        <th>Pseudo</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users %}
                        <tr>
                            <td>
                                <a href="{{ path('user_show',{'slug':user.slug})}}">
                                    <img class="uk-border-circle" data-src="{{asset('img/uploads/' ~ user.image)}}" width="40" alt="{{user.imageCaption}}" uk-img>
                                </a>
                            </td>

                            <td>{{ user.email }}</td>
                            <td>{{ user.roles |json_encode  }}</td>
                            <td>{{ user.pseudo }}</td>
                            <td>
                                <div class="uk-grid-small uk-grid-divider" uk-grid>
                                    <a class="uk-button uk-button-link" href="{{ path('user_edit', {'slug': user.slug}) }}" uk-icon="icon:file-edit" uk-tooltip="title: Modifier {{user.pseudo}}"></a>
                                    <a href="#delete-confirm" uk-toggle>
                                        <span uk-icon="icon: trash" uk-tooltip="title: Supprimer {{user.pseudo}}"></span>
                                    </a>
                                </div>
                                {{ include('user/_delete_form.html.twig')}}</td>
                        </tr>
                    </tbody>
                </tbody>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
    </tbody>
</table></div></section>{% endblock %}
