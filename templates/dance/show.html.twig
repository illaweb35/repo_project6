{% extends 'base.html.twig' %}

{% block title %}Dance{% endblock %}

{% block body %}
    <h1>Dance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ dance.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ dance.slug }}</td>
            </tr>
            <tr>
                <th>SubTitle</th>
                <td>{{ dance.subTitle }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ dance.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ dance.image }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dance.createdAt ? dance.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ dance.updatedAt ? dance.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ImageCaption</th>
                <td>{{ dance.imageCaption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dance_index') }}">back to list</a>

    <a href="{{ path('dance_edit', {'id': dance.id}) }}">edit</a>

    {{ include('dance/_delete_form.html.twig') }}
{% endblock %}
